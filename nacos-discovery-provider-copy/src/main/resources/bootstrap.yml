spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # nacos注册发现服务地址
      config:
        server-addr: 127.0.0.1:8848 # nacos配置服务地址
        file-extension: yaml # nacos config的文件扩展名，影响dataId的生成
server:
  port: 8087